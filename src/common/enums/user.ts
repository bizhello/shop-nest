export enum UserEnum {
  ERROR_USER_NOT_FOUND = 'Такого пользователя не существует',
  MESSAGE_USER_DELETE = 'Пользователь удален',
  USERS = 'users',
  USER = 'User',
  BY_ID = ':id',
}

export default UserEnum;
